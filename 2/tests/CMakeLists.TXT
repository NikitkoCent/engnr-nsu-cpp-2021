add_executable(tests_calc
        tests.cpp
        )

target_link_libraries(tests_calc
        calc_functions
        gtest
        gmock_main
        )

target_include_directories(tests_calc
        PRIVATE "../libs/googletest/googletest/include"
        )

enable_testing()
add_test(NAME googletest COMMAND tests_calc) 